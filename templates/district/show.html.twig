{% extends 'base.html.twig' %}

{% block title %}District{% endblock %}

{% block body %}
    <h1>District</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ district.id }}</td>
            </tr>
            <tr>
                <th>District</th>
                <td>{{ district.district }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('district_index') }}">Retour</a>

    <a href="{{ path('district_edit', {'id': district.id}) }}">Modification</a>

    {{ include('district/_delete_form.html.twig') }}
{% endblock %}
