<form method="post" action="{{ path('bureau_delete', {'id': bureau.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bureau.id) }}">
    <button class="btn">Delete</button>
</form>
