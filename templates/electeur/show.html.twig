{% extends 'base.html.twig' %}

{% block title %}Electeur{% endblock %}

{% block body %}
    <h1>Electeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ electeur.id }}</td>
            </tr>
            <tr>
                <th>Electeur</th>
                <td>{{ electeur.electeur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('electeur_index') }}">Retour</a>

    <a href="{{ path('electeur_edit', {'id': electeur.id}) }}">Modifier</a>

    {{ include('electeur/_delete_form.html.twig') }}
{% endblock %}
