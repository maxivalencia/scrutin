<form method="post" action="{{ path('mode_delete', {'id': mode.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce mode?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mode.id) }}">
    <button class="btn">Supprimer</button>
</form>
