<form method="post" action="{{ path('code_delete', {'id': code.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce code?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ code.id) }}">
    <button class="btn">Supprimer</button>
</form>
