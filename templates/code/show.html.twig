{% extends 'base.html.twig' %}

{% block title %}Code{% endblock %}

{% block body %}
    <h1>Code</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ code.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ code.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('code_index') }}">Retour</a>

    <a href="{{ path('code_edit', {'id': code.id}) }}">Modifier</a>

    {{ include('code/_delete_form.html.twig') }}
{% endblock %}
