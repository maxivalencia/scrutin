{% extends 'base.html.twig' %}

{% block title %}Commune{% endblock %}

{% block body %}
    <h1>Commune</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commune.id }}</td>
            </tr>
            <tr>
                <th>Commune</th>
                <td>{{ commune.commune }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commune_index') }}">Retour</a>

    <a href="{{ path('commune_edit', {'id': commune.id}) }}">Modifier</a>

    {{ include('commune/_delete_form.html.twig') }}
{% endblock %}
