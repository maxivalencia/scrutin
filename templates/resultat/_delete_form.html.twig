<form method="post" action="{{ path('resultat_delete', {'id': resultat.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce résultat?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resultat.id) }}">
    <button class="btn">Supprimer</button>
</form>
