{% extends 'base.html.twig' %}

{% block title %}Vote{% endblock %}

{% block body %}
    <h1>Vote</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vote.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ vote.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vote_index') }}">Retour</a>

    <a href="{{ path('vote_edit', {'id': vote.id}) }}">Modification</a>

    {{ include('vote/_delete_form.html.twig') }}
{% endblock %}
