<form method="post" action="{{ path('vote_delete', {'id': vote.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce vote?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vote.id) }}">
    <button class="btn">Supprimer</button>
</form>
