{% extends 'base.html.twig' %}

{% block title %}Liste des fokontany{% endblock %}

{% block body %}
    <h1>Liste des fokontany</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fokontany</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fokontany in fokontanies %}
            <tr>
                <td>{{ fokontany.id }}</td>
                <td>{{ fokontany.fokontany }}</td>
                <td>
                    <a href="{{ path('fokontany_show', {'id': fokontany.id}) }}">voir</a>
                    <a href="{{ path('fokontany_edit', {'id': fokontany.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(fokontanies) }}
    </div>

    <a href="{{ path('fokontany_new') }}">Ajouter</a>
{% endblock %}
