{% extends 'base.html.twig' %}

{% block title %}Fokontany{% endblock %}

{% block body %}
    <h1>Fokontany</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fokontany.id }}</td>
            </tr>
            <tr>
                <th>Fokontany</th>
                <td>{{ fokontany.fokontany }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fokontany_index') }}">Retour</a>

    <a href="{{ path('fokontany_edit', {'id': fokontany.id}) }}">Modification</a>

    {{ include('fokontany/_delete_form.html.twig') }}
{% endblock %}
